# Contributing
Use feature branches and pull requests. Keep commits small. Add tests for behavior changes. Do not commit secrets or real banking data. Suggested commit style: `feat:`, `fix:`, `docs:`, `test:`, `chore:`.

